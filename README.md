# GibProxies
Docker Compose setup for smart HTTP proxy routing: .onion → Tor (over VPN), YouTube → VPN proxy, everything else direct. Uses dumbproxy JS routing + Gluetun VPN + Tor SOCKS. Perfect for selective traffic tunneling without full VPN overhead.
